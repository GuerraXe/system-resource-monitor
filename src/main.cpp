#include <iostream>

// Milestone 0 placeholder entry point. Argument parsing, the monitor engine,
// and the presentation layer are wired in during later milestones.
int main() {
    std::cout << "System Resource Monitor - build scaffold OK\n";
    return 0;
}
